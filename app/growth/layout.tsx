import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard Comercial — Closeup Marketing",
};

export default function GrowthLayout({ children }: { children: React.ReactNode }) {
  return children;
}
